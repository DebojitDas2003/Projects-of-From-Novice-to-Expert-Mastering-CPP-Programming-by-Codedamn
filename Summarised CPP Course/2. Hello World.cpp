#include <iostream>
//#include is a preprocessor that tells the compiler which libraries to include before running the program
using namespace std;


int main() {
//"main()" function is the starting point of the program
    cout << "Hello World";
//"cout" is a object from istream class and "Hello World" is a expression, Statement and expression work together to make things work.
    return 0;
//"return" delivers the result of the program. If it's successful or not.
}